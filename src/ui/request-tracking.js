const statusSteps=[['request','Solicitação recebida','Seu pedido foi registrado e está aguardando análise.'],['quote','Orçamento','O valor e as condições serão definidos para sua aprovação.'],['schedule','Agendamento','Data e período do atendimento serão confirmados.'],['work_order','Ordem de serviço','O atendimento será liberado para execução.'],['execution','Em execução','O serviço estará em andamento.'],['payment','Pagamento','Pagamento será liberado após a conclusão.'],['review','Avaliação','Conte como foi sua experiência.']];
function esc(v=''){const e=document.createElement('div');e.textContent=v;return e.innerHTML}
function id(){return `SS-${String(Date.now()).slice(-6)}`}
export function initRequestTracking(){let current=null;const root=document.createElement('section');root.className='tracking-view';root.hidden=true;root.innerHTML=`<header class="tracking-view__top"><button class="tracking-view__back" type="button" aria-label="Voltar"><i class="ri-arrow-left-line"></i></button><div><strong>Acompanhar solicitação</strong><span id="tracking-id"></span></div></header><main class="tracking-view__body"><div class="tracking-hero"><span class="tracking-hero__badge"><i class="ri-time-line"></i> Em análise</span><h2 id="tracking-service"></h2><p>Você poderá acompanhar cada etapa do atendimento por aqui.</p></div><div class="tracking-summary" id="tracking-summary"></div><section class="tracking-section"><span class="tracking-section__label">Jornada do atendimento</span><div class="tracking-timeline" id="tracking-timeline"></div></section><div class="tracking-note"><i class="ri-information-line"></i><span>Este acompanhamento é uma demonstração frontend. Os status serão atualizados em tempo real quando o backend for conectado.</span></div><button class="tracking-home" type="button">Voltar aos serviços</button></main>`;document.body.appendChild(root);const timeline=root.querySelector('#tracking-timeline');function render(){if(!current)return;root.querySelector('#tracking-id').textContent=current.id;root.querySelector('#tracking-service').textContent=current.service;root.querySelector('#tracking-summary').innerHTML=`<div><small>Data solicitada</small><strong>${esc(current.date)}</strong></div><div><small>Período</small><strong>${esc(current.period)}</strong></div><div><small>Local</small><strong>${esc(current.location)}</strong></div>`;timeline.innerHTML=statusSteps.map((step,index)=>`<article class="tracking-step ${index===0?'active':''}"><div class="tracking-step__marker"><i class="${index===0?'ri-checkbox-circle-fill':'ri-circle-line'}"></i></div><div><small>${String(index+1).padStart(2,'0')}</small><strong>${step[1]}</strong><p>${step[2]}</p>${index===0?'<span>Agora</span>':''}</div></article>`).join('')}
 function close(){root.hidden=true;document.body.style.overflow='';document.querySelector('#services')?.scrollIntoView({behavior:'smooth'})}root.querySelector('.tracking-view__back').addEventListener('click',close);root.querySelector('.tracking-home').addEventListener('click',close);return {open(data){current={id:id(),...data};render();root.hidden=false;document.body.style.overflow='hidden';root.querySelector('.tracking-view__body').scrollTop=0},getCurrent:()=>current}}
